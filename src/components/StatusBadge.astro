---
/**
 * StatusBadge — renders a worn analog-label style badge for project status.
 * Styled to look like a classification stamp or mission-document marking.
 */
export type Status = 'live' | 'beta' | 'in-progress' | 'planning';

interface Props {
  status: Status;
}

const { status } = Astro.props;

const config: Record<Status, { label: string; classes: string }> = {
  live: {
    label: 'LIVE',
    classes: 'text-moss-dark dark:text-moss-light border-moss dark:border-moss-light bg-moss-dark/10 dark:bg-moss-light/10',
  },
  beta: {
    label: 'BETA',
    classes: 'text-amber-dark dark:text-amber-light border-amber dark:border-amber-light bg-amber-dark/10 dark:bg-amber-light/10',
  },
  'in-progress': {
    label: 'IN PROGRESS',
    classes: 'text-rust dark:text-rust-light border-rust dark:border-rust-light bg-rust/10 dark:bg-rust-light/10',
  },
  planning: {
    label: 'PLANNING',
    classes: 'text-paper-600 dark:text-paper-400 border-paper-400 dark:border-paper-500 bg-paper-300/50 dark:bg-paper-700/30',
  },
};

const { label, classes } = config[status];
---

<!--
  Border + all-caps monospace gives the "rubber stamp" look that fits
  the 70s mission-document aesthetic without heavy background fills.
-->
<span
  class:list={[
    'font-mono text-[10px] tracking-[0.15em] border px-1.5 py-0.5 uppercase',
    classes,
  ]}
>
  {label}
</span>
